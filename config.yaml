port: 7890
socks-port: 7891
allow-lan: false
mode: global  # ← 改为全局模式测试
log-level: debug  # ← 开启详细日志

dns:
  enable: true
  nameserver:
    - 8.8.8.8
    - 1.1.1.1

proxies:
  # 只测试 Comcast
  - name: "Comcast"
    type: http  # 或 socks5
    server: "46.34.39.123"
    port: 43321
    username: "WA8eNELH64mNmhp"
    password: "bWPDb9vGglhV4t1"

proxy-groups:
  - name: "代理"
    type: select
    proxies:
      - "Comcast"

rules:
  - MATCH,Comcast
